# Proyecto Netlify: Root directory = tucoachfront (en el panel de Netlify)
[build]
  command = "pnpm install && pnpm build"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
